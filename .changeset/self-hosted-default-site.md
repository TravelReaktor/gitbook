---
"gitbook": patch
---

Add `GITBOOK_DEFAULT_SITE` to self-host a single published site at the server root, and fix an unhandled rejection when concurrent requests shared a failed in-flight icon fetch.
